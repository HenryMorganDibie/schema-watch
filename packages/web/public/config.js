// Runtime API endpoint. Edit this one line and redeploy to repoint the app at
// a different backend - no rebuild needed. Empty means same-origin, which is
// what local dev uses (Vite proxies /api to localhost:4000).
window.__SCHEMA_WATCH_API__ = "https://schema-watch-server-sigma.vercel.app";
