{
  "name": "schema-watch",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "demo": "node examples/demo/run-demo.mjs",
    "build": "npm run build --workspaces --if-present && npm run bundle --workspace packages/agent",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:agent": "npm run dev --workspace packages/agent",
    "dev:dashboard": "npm run dev --workspace packages/dashboard",
    "dev:server": "npm run dev --workspace packages/server",
    "db:migrate": "npm run db:migrate --workspace packages/server",
    "db:generate": "npm run db:generate --workspace packages/server",
    "dev:web": "npm run dev --workspace packages/web",
    "vercel:web": "npm run build --workspace packages/core && npm run build --workspace packages/web",
    "vercel:server": "npm run build --workspace packages/core && npm run db:generate --workspace packages/server && npm run build --workspace packages/server"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
